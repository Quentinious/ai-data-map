# Windows IPv6 localhost can sometimes hang or timeout (wslrelay issue).
# Use 127.0.0.1 instead of localhost to avoid potential DNS/IPv6 resolution delays.
VITE_BACKEND_BASE_URL=http://127.0.0.1:4000
